# Nbcl REPL

Read-eval-print-loop for nbcl. It can be useful for quick tests of sorts.

**Features:**

- Syntax highlighting
- History
- Brace Validator

## Usage

Just run the compiled binary and run `help()` to get started.
